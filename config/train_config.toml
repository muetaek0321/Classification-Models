output_path = "./results"
gpu = 0
use_pretrained = true

# 使用するモデルの名前
model_name = "EfficientNetV2"
#model_name = "VisionTransformer"
#model_name = "DeiT"

[parameters]
num_epoches = 100
batch_size = 32

# 入力画像サイズ: (height, width)
input_size = [224, 224]

[optimizer]
lr = 1e-4